services:
  app:
    image: fortune-cat
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment:
      SERVER_NAME: "${SERVER_NAME}"
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    restart: unless-stopped